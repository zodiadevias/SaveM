export interface Store {
  id?: string;            // Firebase Auth UID
  logoUrl?: string;       // Store logo URL
  ownerId?: string;       // Firebase Auth UID\
  storeAddress?: object;  // Store address
  storeName?: string;     // Store name
}